diff a/backend/app/db/session.py b/backend/app/db/session.py	(rejected hunks)
@@ -10,6 +10,7 @@ from sqlalchemy.ext.asyncio import (
     create_async_engine,
 )
 from sqlalchemy.exc import SQLAlchemyError, DisconnectionError
+from fastapi import HTTPException
 
 from ..core.config import settings
 
@@ -68,6 +69,22 @@ async def get_session() -> AsyncIterator[AsyncSession]:
                     except Exception as rollback_exc:
                         db_logger.error(f"Failed to rollback session: {rollback_exc}")
                 raise
+            except HTTPException as exc:
+                status_code = getattr(exc, "status_code", None)
+                if status_code is not None and 400 <= status_code < 500:
+                    db_logger.info(
+                        "HTTPException in session: %s (status=%s)",
+                        exc.detail,
+                        status_code,
+                    )
+                else:
+                    db_logger.error(
+                        "HTTPException in session: %s (status=%s)",
+                        exc.detail,
+                        status_code,
+                        exc_info=True,
+                    )
+                raise
             except Exception as exc:
                 db_logger.error(
                     f"Unexpected error in session: {type(exc).__name__}: {exc}",
